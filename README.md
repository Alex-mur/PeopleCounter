# PeopleCounter 🚦

**PeopleCounter** — это распределенная система подсчета посетителей в зонах интереса на основе видеопотоков, использующая алгоритмы компьютерного зрения и микросервисную архитектуру.

Система состоит из трех основных компонентов:
1. **Manager (Backend)** — центральный сервер управления на Go.
2. **WorkerYOLO26 (AI/ML Service)** — сервис видеоаналитики на Python (YOLO).
3. **Front (Frontend)** — пользовательский веб-интерфейс на Vue 3.

---

## 🏗 Архитектура проекта

### 1. ⚙️ Manager (`PeopleCounter_Manager`)
Центральный управляющий сервис, написанный на **Go 1.24**.
- **Функции:** Управление счетчиками посетителей, сбор статистики, агрегация данных и аутентификация пользователей.
- **Технологии:** Go-chi, PostgreSQL (через pgx), JWT Auth.
- **API:** REST API с поддержкой Swagger/OpenAPI (доступно по умолчанию на порту `9000`).

### 2. 🧠 Worker (`PeopleCounter_WorkerYOLO26`)
Веб-сервис для обработки видеопотоков и детекции людей, написанный на **Python**.
- **Функции:** Захват видео (IP-камеры, RTSP, видеофайлы), детекция людей с использованием моделей YOLO, подсчет пересечений заданных линий.
- **Технологии:** OpenCV, Ultralytics YOLO, FastAPI/Flask, DuckDB (для аналитики и локального хранения).
- **Особенности:** Поддержка Multistreaming, автоматический перезапуск при разрыве соединения с камерой, автоочистка логов.

### 3. 🖥 Frontend (`PeopleCounter_Front`)
Пользовательский интерфейс, разработанный на **Vue 3** (Composition API) и Vite.
- **Функции:** Визуализация статистики подсчета, управление сервисами, настройка линий подсчета, панель авторизации (Login, Management, Statistics, ServiceCounters).
- **Технологии:** Vue 3, Vue Router, Pinia, Nginx (для продакшен сборки).

---

## 🚀 Установка и запуск

Проект спроектирован для запуска через **Docker** и **Docker Compose**. Вы можете запустить всю систему целиком с помощью скрипта автоматизации или поднять каждый сервис по отдельности.

### Предварительные требования:
- Установленный Docker и Docker Compose.

---

### Вариант 1: Автоматизированный запуск всей системы (Рекомендуется)

В корне репозитория находится скрипт `build.sh`, который автоматически генерирует файлы конфигурации, настраивает порты, поднимает базу данных PostgreSQL и собирает все сервисы (Manager, Worker, Front).

1. **Настройте переменные (опционально):**
   Откройте файл `build.sh` и в блоке `--- ОСНОВНЫЕ НАСТРОЙКИ СИСТЕМЫ ---` укажите нужный IP-адрес сервера (`SERVER_IP`), порты и реквизиты для базы данных.
   
2. **Запустите скрипт:**
   В терминале, находясь в корне проекта, выполните:
   ```bash
   chmod +x build.sh
   ./build.sh
   ```

Скрипт автоматически создаст `.env` файл, подставит пароли к БД, сгенерирует `config.json` для менеджера и `config.ini` для воркера, а затем запустит `docker-compose up -d --build`.

После успешного запуска:
- **Frontend** будет доступен по адресу: `http://<SERVER_IP>:8080`
- **Swagger API** (Manager): `http://<SERVER_IP>:9000/swagger/index.html`
- **БД PostgreSQL** будет поднята на порту `5434`.

---

### Вариант 2: Ручная сборка по отдельности

Если вы не хотите использовать общую сборку, вы можете запустить компоненты вручную. 
*Примечание: в этом случае вам потребуется самостоятельно развернуть PostgreSQL для менеджера.*

**1. Manager:**
```bash
cd PeopleCounter_Manager
# Скопируйте и настройте конфигурацию
cp example.config.json config.json 
# В config.json укажите ваши актуальные данные для подключения к БД и порты
docker-compose up -d --build
```

**2. Worker:**
```bash
cd PeopleCounter_WorkerYOLO26
# Создайте конфигурационный файл
cp config.ini.example config.ini
# Настройте видеопотоки, зоны, порт и api_key в config.ini
docker-compose up -d --build
```

**3. Frontend:**
```bash
cd PeopleCounter_Front
docker-compose up -d --build
```
*Для корректной работы Frontend соберите его с указанием правильного IP-адреса менеджера в параметре `VITE_API_BASE_URL` внутри его `docker-compose.yml`.*

---

## 📚 Документация API

Документация к API менеджера сгенерирована с помощью Swagger.
После запуска сервиса Manager, документация доступна по пути:
`http://<manager-host>:9000/swagger/index.html`

---

## 📝 Лицензия

Проект распространяется на условиях открытой лицензии (см. файл [LICENSE](./LICENSE) в корне репозитория).
