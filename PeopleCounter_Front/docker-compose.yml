services:
  peoplecounter-front:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=http://192.168.1.103:9000/
    container_name: peoplecounter-front
    restart: unless-stopped
    ports:
      - "8080:80"
